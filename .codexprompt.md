Always read and strictly adhere to the rules outlined in `AGENTS.md` before executing any tasks.

When troubleshooting cross-repo interoperability or container issues using your SSH MCP tools:
1. Cross-reference the repository paths mentioned in `AGENTS.md` (e.g., `mkwV2` for core backend/API, `printlab` for Bambu/printer controls, `stockworks` for material inventory).
2. Adhere strictly to the "Safety Rules" section:
   - Never print database URLs or `.env` contents to the chat window.
   - Seek explicit confirmation before triggering `docker_restart` or passing any active commands to real physical hardware via `printlab` loops.
3. If an upload fails or data fails to synchronize, use `docker_logs` to cross-examine `mkwV2`, `stockworks`, and `printlab` containers sequentially to locate the network or state failure.