ALTER TABLE "ProductTemplate"
  ADD COLUMN IF NOT EXISTS "lockedMaterial" TEXT DEFAULT 'PLA',
  ADD COLUMN IF NOT EXISTS "lockedColor" TEXT,
  ADD COLUMN IF NOT EXISTS "lockedColorCount" INTEGER NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS "lockedScale" DOUBLE PRECISION NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS "lockedFinish" TEXT DEFAULT 'standard',
  ADD COLUMN IF NOT EXISTS "lockedPriceMultiplier" DOUBLE PRECISION NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS "stockworksMaterialId" INTEGER,
  ADD COLUMN IF NOT EXISTS "stockworksInventoryItemId" INTEGER;
